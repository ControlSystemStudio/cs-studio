# URL of LDAP server used for authorization
# Leave empty to disable auth, i.e. always allow access
ldap_url=ldap://localhost:389/ou=CSSAuthorization,dc=test,dc=ics

# Name and password used for connection.
# Leave empty for anonymous access (read-only)
ldap_user=
ldap_password=