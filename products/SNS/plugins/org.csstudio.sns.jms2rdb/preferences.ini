## Default preferences for this plugin

# Port for built-in HTTPD
httpd_port=4913

# JMS URL where tool listens for messages to log
jms_url=failover:(tcp://ics-srv02.sns.ornl.gov:61616,tcp://ics-srv-epics1.ics.sns.gov:61616)

# List of topics to log, separated by ','
jms_topic=LOG,ALARM_SERVER,ALARM_CLIENT,TALK

# Filters for suppressed JMS messages, format
# <Type>;<Property>=<Pattern>, <Type>;<Property>=<Pattern>
# with message 'TYPE' property, additional property name, regex pattern for value
jms_filters=ALARM;TEXT=IDLE, LOG;TEXT=JCACommandThread queue reached

# MySQL Tests
#rdb_url=jdbc:mysql://localhost/log?user=log&password=$log
#rdb_url=jdbc:mysql://titan-terrier.sns.ornl.gov/log?user=log&password=$log
#rdb_schema=

# SNS; Can't put that PW into CVS...
#rdb_url=jdbc:oracle:thin:epics_mon/PASSWORD@//snsdev3.sns.ornl.gov:1521/devl
rdb_url=jdbc:oracle:thin:epics_mon/PASSWORD@//snsdb1.sns.ornl.gov:1521/prod
rdb_schema=EPICS



