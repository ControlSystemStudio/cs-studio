#ANT BUILD SPECIFIC PROPERTIES#





# PDE Build with P2
p2.gathering=true

# Base Folder 
base=${basedir}

# Buckminster Config
buckminsterCquery=${base}/feature.cquery

# Workspace folder
workspaceLocation=${hudsonbase}/home/jobs/${hudsonjob}/workspace/

#TEST PROPERTIES
# Test related folders
testDirectory=${base}/test-dir
testResultsDirectory=${base}/test-reports

# Test related plug-ins classes
testPlugin=org.csstudio.testsuite
testClassname=org.csstudio.testsuite.AllTestsSuite

# Test Target (ui-test or core-test)
testtarget=ui-test

# Test related environment settings
#os=win32
#ws=win32
#arch=x86



# PDE BUILD SPECIFIC PROPERTIES #


#Target Platform for TMP build
e360TP=${hudsonbase}/EclipseTargetPlatform/e360
eclipse360=${hudsonbase}/eclipse360ForHeadlessBuild


# Eclipse Target
baseLocation=${e360TP}
buildDirectory=D:/Java/hudson/home/jobs/McssIsland/workspace/build


#The type of the top level element we are building,  generally feature
#topLevelElementType = feature
#The id of the top level element we are building
#topLevelElementId = org.foo.bar


# PRODUCTPACKAGING CONTROL
product=/org.csstudio.desy.island.product/island.product
runPackager=true

# The prefix that will be used in the generated archive.
archivePrefix=CSS_Island

# The location underwhich all of the build output will be collected.
collectingFolder=${archivePrefix}

# The list of {os, ws, arch} configurations to build.  
#configs=win32, win32, x86
#configs=win32, win32, x86

#Arguments to send to the zip executable
zipargs=

#Arguments to send to the tar executable
tarargs=


#BUILD NAMING CONTROL
# The directory into which the build elements are fetched and where the build takes place.
#buildDirectory=/build
#buildDirectory=${hudsonbase}/home/jobs/${hudsonjob}/workspace/build
#buildDirectory=${hudsonbase}

# Type of build.  Used in naming the build output. 
buildType=I

# ID of the build.  Used in naming the build output.
buildId=CSS_Island

# Label for the build.  Used in naming the build output
buildLabel=${buildType}.${buildId}

# Timestamp for the build.  Used in naming the build output
timestamp=007

#this property indicates whether you want the set of plug-ins and features to be considered during the build to be limited to the ones reachable from the features  plugins being built
filteredDependencyCheck=false

#this property indicates whether the resolution should be done in development mode (i.e. ignore multiple bundles with singletons)
resolution.devMode=false

skipBase=true
eclipseURL=url for eclipse download site
eclipseBuildId=Id of Eclipse build to get
eclipseBaseURL=${eclipseURL}/eclipse-platform-${eclipseBuildId}-win32.zip



#MAP FILE CONTROL
skipMaps=true
mapsRepo=pserveranonymous@example.com/pathtorepo
mapsRoot=pathtomaps
mapsCheckoutTag=HEAD

#tagMaps=true
mapsTagTag=v${buildId}



#REPOSITORY CONTROL
skipFetch=true



# JAVA COMPILER OPTIONS
# Specify the output format of the compiler log when eclipse jdt is used
logExtension=.log

# Whether or not to include debug info in the output jars
javacDebugInfo=false 

# Whether or not to fail the build if there are compiler errors
javacFailOnError=true

# Enable or disable verbose mode of the compiler
javacVerbose=true

# Default value for the version of the source code. This value is used when compiling plug-ins that do not set the Bundle-RequiredExecutionEnvironment or set javacSource in build.properties
javacSource=1.6

# Default value for the version of the byte code targeted. This value is used when compiling plug-ins that do not set the Bundle-RequiredExecutionEnvironment or set javacTarget in build.properties.
javacTarget=1.6

#eclipse.pdebuild.scripts=${eclipse360}/plugins/org.eclipse.pde.build_3.6.0.v20100603/scripts
#eclipse.pdebuild.templates=${eclipse360}/plugins/org.eclipse.pde.build_3.6.0.v20100603/templates