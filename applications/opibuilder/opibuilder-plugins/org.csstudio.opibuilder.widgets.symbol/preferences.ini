# Regular expression applied as filter for BOY Symbol Image browsing
filter_regex=

# RGB color code in SVG files
# To be replaced dynamically by the On(zero)/Off(non-zero) color at runtime 
# By default the black color will be changed
color_to_change=0,0,0
