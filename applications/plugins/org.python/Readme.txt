Jython Support
==============

Plugin is called org.python because that is 
the name used by many of the classes in jython.jar
as well as the group used for maven packaging.

Jar was created from the jython_installer-<version of this plugin>.jar
for a "standalone (callable jar file)" installation.
