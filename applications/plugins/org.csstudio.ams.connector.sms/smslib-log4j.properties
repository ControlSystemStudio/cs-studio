# An example log4j configuration file that outputs to System.out.  The
# output information consists of relative time, log level, thread
# name, logger name, nested diagnostic context and the message in that
# order.

# For the general syntax of property based configuration files see the
# documenation of org.apache.log4j.PropertyConfigurator.

log4j.rootLogger=DEBUG,Logdatei

# A1 is set to be a ConsoleAppender which outputs to System.out. 
log4j.appender.Logdatei=org.apache.log4j.RollingFileAppender
log4j.appender.Logdatei.File=smslib/smslib-351.log
log4j.appender.Logdatei.MaxFileSize=2048KB
log4j.appender.Logdatei.MaxBackupIndex=100

# A1 uses PatternLayout.
log4j.appender.Logdatei.layout=org.apache.log4j.PatternLayout

# The conversion pattern uses format specifiers. You might want to
# change the pattern an watch the output format change.
log4j.appender.Logdatei.layout.ConversionPattern=%d{ISO8601} %-5p [%t] %c: %m%n
