# Default Preferences

# define the rate at which the logs should be automaticallu refreshed with the view is active.
# time is specified in minutes and 0 implies to disable auto refresh
auto.refresh.rate=1